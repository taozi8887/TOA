## ✨ TOA v0.5.0 - Auto-Update System

### 🚀 Major Features

**Auto-Update System with GUI Installer**
- 🔄 Automatic update checks on every launch
- 📦 GUI installer window (600x400) with progress bar
- ⚡ Downloads only changed files to minimize bandwidth
- 🔧 Updates game code, levels, beatmaps, and assets from GitHub
- 📥 First-run automatic installation of all game files
- 🔗 Launcher pattern for code hot-swapping without rebuilding exe

**Professional Appearance**
- 👁️ Hidden console window during installation and gameplay
- 📁 Hidden .toa folder on Windows (contains runtime files)
- 🎨 Clean GUI installer with file information and progress tracking

**Developer Improvements**
- 📝 Dynamic version reading in build_exe.py from TOA.spec
- ⚙️ Optimized update workflow with update.py script
- 🚀 One-command push: `python update.py "commit message"`
- 🔐 Automatically updates version.json with only changed files
- 🌐 Uses GitHub raw URLs for hash calculation

### 🔧 Changes

- **Runtime Generation**: Levels and beatmaps now generated from .osz files instead of pre-generated JSON
- **Simplified Updates**: Always sync with GitHub (no hash verification during updates)
- **Cleaner Code**: Removed redundant update check from main.py (launcher handles all updates)
- **UI Cleanup**: Removed auto-update test text overlay from gameplay

### 🐛 Bug Fixes

- Pygame DLL loading issues resolved with pre-load strategy
- Path separator issues fixed (standardized to forward slashes)
- Hash mismatch issues resolved by calculating hashes from GitHub-served files
- Restart logic improved to prevent pygame temp directory issues

### 📊 Technical Details

- **Tracked Files**: 26 files total (5 Python files, 8 images, 13 .osz files)
- **Hash Verification**: SHA256 for version tracking
- **Supported Extensions**: .json, .osu, .mp3, .wav, .ogg, .jpg, .png, .osz
- **Update System**: Launcher shows GUI installer for first run and updates
- **Console**: Hidden via console=False in TOA.spec

───────────────────────────────────────────────────────────────────

**Download TOA-v0.5.0.exe** and enjoy automatic updates!

**Note**: On first run, the game will automatically download all required files.
Make sure you have an internet connection. Updates will happen automatically
on each launch, downloading only files that have changed.
